
stages:
  - build
  - deploy

build:linux:386:
  stage: build
  script:
    - export CI_OS=linux
    - export CI_ARCH=386
    - scripts/ci-build.sh
  tags:
    - docker-itchsetup-32
  artifacts:
    paths:
      - binaries

build:linux:amd64:
  stage: build
  script:
    - export CI_OS=linux
    - export CI_ARCH=amd64
    - scripts/ci-build.sh
  tags:
    - docker-itchsetup-64
  artifacts:
    paths:
      - binaries

build:windows:386:
  stage: build
  script:
    - export CI_OS=windows
    - export CI_ARCH=386
    - scripts/ci-build.sh
  tags:
    - windows
  artifacts:
    paths:
      - binaries

build:darwin:amd64:
  stage: build
  script:
    - export CI_OS=darwin
    - export CI_ARCH=amd64
    - scripts/ci-build.sh
  tags: 
    - darwin
  artifacts:
    paths:
      - binaries

deploy:
  stage: deploy
  tags:
    - dropsy
  script:
    - scripts/ci-deploy.sh
